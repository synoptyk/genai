diff --git a/.env.example b/.env.example
index c583ca2a7..08de6e848 100644
--- a/.env.example
+++ b/.env.example
@@ -1,40 +1,228 @@
-# ================================
-# Gen AI - Environment Template
-# Copy to .env and adjust values
-# ================================
+# ════════════════════════════════════════════════════════════════════════════
+# 🔐 GEN AI - ENVIRONMENT CONFIGURATION TEMPLATE
+# ════════════════════════════════════════════════════════════════════════════
+#
+# ⚠️  IMPORTANTE: Este archivo NO contiene valores reales
+# 🚨 NUNCA comitees .env - Solo .env.example en repositorio
+#
+# Para configurar:
+#   1. Copia este archivo: cp .env.example .env
+#   2. Rellena con valores REALES de tu infraestructura
+#   3. NUNCA hagas push de .env
+#   4. Usa gestor de secretos para producción
+#
+# ════════════════════════════════════════════════════════════════════════════
 
-# Core
-NODE_ENV=production
+# ════════════════════════════════════════════════════════════════════════════
+# 🌍 ENTORNO Y PUERTO
+# ════════════════════════════════════════════════════════════════════════════
+
+# Opciones: development, staging, production
+NODE_ENV=development
+
+# Puerto donde corre el servidor
 PORT=5003
-PLATFORM_PATH=./platforms/agentetelecom
 
-# URLs
-FRONTEND_URL=http://localhost:3000
-REACT_APP_API_URL=http://localhost:5003
+# ════════════════════════════════════════════════════════════════════════════
+# 🗃️ BASE DE DATOS - MongoDB
+# ════════════════════════════════════════════════════════════════════════════
+
+# ⚠️  CRÍTICO: Esta es la clave más importante
+# Formato: mongodb://user:password@host:port/database
+# Para producción: Usa MongoDB Atlas con autenticación fuerte
+# - Usuario: No usar "admin"
+# - Contraseña: Mínimo 32 caracteres, caracteres especiales
+# - Conexión: Use IP whitelist y SSL/TLS
+#
+# Ejemplo (NO USAR):
+#   mongodb://myuser:MyPassword123!@34.27.229.165:27017/genai?authSource=admin
+#
+# Mejor (Usar):
+#   mongodb+srv://user:pass@cluster.mongodb.net/genai?retryWrites=true&w=majority
+
+MONGO_URI=mongodb://user:password@localhost:27017/genai_db
 
-# Database
-# Docker local default:
-MONGO_URI=mongodb://mongo:27017/genai_db
-# Atlas example:
-# MONGO_URI=mongodb+srv://user:pass@cluster.mongodb.net/genai_db?retryWrites=true&w=majority
+# ════════════════════════════════════════════════════════════════════════════
+# 🔑 SEGURIDAD Y AUTENTICACIÓN
+# ════════════════════════════════════════════════════════════════════════════
 
-# Auth
-JWT_SECRET=change-this-secret-in-production
+# ⚠️  CRÍTICO: Clave para firmar JWT tokens
+# - Mínimo: 32 caracteres
+# - Debe ser: Aleatorio, seguro, único por ambiente
+# - Generar con: openssl rand -base64 32
+# - NO usar: "my-secret" o valores predecibles
+#
+# PRODUCCIÓN: Cambiar cada 30 días
+
+JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-random
+
+# Duración de los tokens (ej: 7d, 24h, 30m)
 JWT_EXPIRE=7d
 
-# SMTP (optional but recommended)
+# ════════════════════════════════════════════════════════════════════════════
+# 🌐 CORS - URLS PERMITIDAS
+# ════════════════════════════════════════════════════════════════════════════
+
+# Frontend URL (para CORS)
+FRONTEND_URL=http://localhost:3000
+
+# URLs adicionales permitidas (separadas por coma)
+# ALLOWED_ORIGINS=https://genai.cl,https://app.genai.cl,https://admin.genai.cl
+
+# ════════════════════════════════════════════════════════════════════════════
+# 📧 SMTP - CONFIGURACIÓN DE EMAIL
+# ════════════════════════════════════════════════════════════════════════════
+
+# Servidor SMTP
 SMTP_HOST=smtp.gmail.com
+
+# Puerto (587 para TLS, 465 para SSL)
 SMTP_PORT=587
-SMTP_USER=
-SMTP_PASSWORD=
 
-# Cloudinary (optional)
-CLOUDINARY_CLOUD_NAME=
-CLOUDINARY_API_KEY=
-CLOUDINARY_API_SECRET=
+# Email que envía emails
+SMTP_EMAIL=your-email@gmail.com
+
+# ⚠️  Usar contraseña de aplicación (NO contraseña de cuenta)
+# - Gmail: Generar en https://myaccount.google.com/apppasswords
+# - Zoho: Generar en https://zoho.com
+SMTP_PASSWORD=your-app-specific-password
+
+# Nombre que aparecerá en los emails
+FROM_NAME=Gen AI Platform
+
+# ════════════════════════════════════════════════════════════════════════════
+# ☁️ CLOUDINARY - ALMACENAMIENTO DE ARCHIVOS
+# ════════════════════════════════════════════════════════════════════════════
+
+# Nombre de la cuenta Cloudinary
+CLOUDINARY_CLOUD_NAME=your-cloud-name
+
+# API Key (pública, pero el secret NO)
+CLOUDINARY_API_KEY=your-api-key
+
+# ⚠️  SECRET: Nunca exponer este valor
+# Generar en: https://cloudinary.com/console
+CLOUDINARY_API_SECRET=your-api-secret
+
+# ════════════════════════════════════════════════════════════════════════════
+# 🧠 GOOGLE GEMINI AI
+# ════════════════════════════════════════════════════════════════════════════
+
+# ⚠️  API Key para Google Gemini
+# - Generar en: https://makersuite.google.com/app/apikey
+# - Requiere: Cuenta Google Cloud con billing habilitado
+# - Costo: Varía según uso (modelo free: 60 requests/minuto)
+# - Seguridad: Mantener en secreto, rotary cada 90 días
+
+GEMINI_API_KEY=AIzaSy_your_key_here_keep_secret
+
+# ════════════════════════════════════════════════════════════════════════════
+# 🤖 GROQ AI - LLM RÁPIDO Y GRATIS
+# ════════════════════════════════════════════════════════════════════════════
 
-# Logging
+# ⚠️  API Key para Groq (Llama 3.3 - modelo gratis)
+# - Generar en: https://console.groq.com
+# - Límite: 14,400 requests/día
+# - Modelo: Llama 3.3 70B
+# - Costo: GRATIS (con límite)
+
+GROQ_API_KEY=gsk_your_key_here_keep_secret
+
+# ════════════════════════════════════════════════════════════════════════════
+# 📱 GOOGLE MAIL OAUTH2 - INTEGRACIÓN DE EMAIL
+# ════════════════════════════════════════════════════════════════════════════
+
+# ⚠️  Credenciales de OAuth de Google
+# - Crear en: https://console.cloud.google.com
+# - Paso 1: Crear nuevo proyecto
+# - Paso 2: Habilitar Gmail API
+# - Paso 3: Crear OAuth 2.0 Client ID (Aplicación web)
+# - Paso 4: Configurar URIs de redirección
+
+# ID del cliente OAuth
+GOOGLE_MAIL_CLIENT_ID=your-client-id.apps.googleusercontent.com
+
+# ⚠️  SECRET de OAuth - MANTENER EN SECRETO
+# No commitear nunca
+GOOGLE_MAIL_CLIENT_SECRET=your-client-secret-keep-secret
+
+# ════════════════════════════════════════════════════════════════════════════
+# 🛠️ CREDENCIALES DE TERCEROS - INTEGRACIÓN TOA
+# ════════════════════════════════════════════════════════════════════════════
+
+# URL del sistema TOA (Telefónica)
+TOA_URL=https://telefonica-cl.etadirect.com/
+
+# ⚠️  Usuario y contraseña TOA
+# - Mantener en secreto
+# - Cambiar regularmente
+# - NO usar en código
+
+TOA_USER_REAL=your-toa-username
+TOA_PASS_REAL=your-toa-password
+
+# ════════════════════════════════════════════════════════════════════════════
+# 🚗 GPS SIMPLE - INTEGRACIÓN DE GEOLOCALIZACIÓN
+# ════════════════════════════════════════════════════════════════════════════
+
+# URL de GPS Simple
+GPS_URL=https://www.gpsimple.cl/track/index.php
+
+# ⚠️  Credenciales GPS
+# - Mantener en secreto
+# - Email de cuenta
+
+GPS_USER=your-email@company.com
+GPS_PASS=your-gps-password
+
+# ════════════════════════════════════════════════════════════════════════════
+# 👤 SUPER ADMIN SEED - USUARIO INICIAL
+# ════════════════════════════════════════════════════════════════════════════
+
+# Email del superadmin (crear primer usuario)
+SEED_ADMIN_EMAIL=admin@company.com
+
+# ⚠️  CONTRASEÑA TEMPORAL - CAMBIAR AL PRIMER LOGIN
+# - Generada aleatoriamente al primer inicio
+# - Forzar cambio de contraseña en primer acceso
+
+SEED_ADMIN_PASSWORD=GeneratedAtFirstRun_ChangeMe
+
+# Habilitar creación automática de admin al startup
+ENABLE_AUTO_SEED=false
+
+# ════════════════════════════════════════════════════════════════════════════
+# 📊 LOGGING
+# ════════════════════════════════════════════════════════════════════════════
+
+# Nivel de log: error, warn, info, debug
 LOG_LEVEL=info
 
-# Allowed origins (optional, comma-separated)
-# ALLOWED_ORIGINS=http://localhost:3000,https://genai.cl
+# ════════════════════════════════════════════════════════════════════════════
+# 🎯 PLATAFORMA
+# ════════════════════════════════════════════════════════════════════════════
+
+# Ruta de la plataforma específica
+PLATFORM_PATH=./platforms/agentetelecom
+
+# ════════════════════════════════════════════════════════════════════════════
+# ✅ CHECKLIST DE SEGURIDAD ANTES DE PRODUCCIÓN
+# ════════════════════════════════════════════════════════════════════════════
+#
+# 📋 Completar ANTES de usar en producción:
+#
+# [ ] JWT_SECRET: ¿Es aleatorio y >32 caracteres?
+# [ ] MONGO_URI: ¿Usa SSL/TLS y IP whitelist?
+# [ ] MONGO_URI: ¿Usuario NO es "admin"?
+# [ ] SMTP_PASSWORD: ¿Es contraseña de aplicación, NO de cuenta?
+# [ ] Todas las claves API: ¿Son de producción?
+# [ ] NODE_ENV: ¿Es "production"?
+# [ ] Archivo .env: ¿NO está en git?
+# [ ] .env.example: ¿Solo contiene valores dummy?
+# [ ] CORS: ¿Está configurado solo para dominios válidos?
+# [ ] Rate limiting: ¿Habilitado?
+# [ ] Helmet security: ¿Habilitado?
+# [ ] Logs: ¿No contienen información sensible?
+#
+# ════════════════════════════════════════════════════════════════════════════
+
diff --git a/.gitignore b/.gitignore
index fa67074c9..401718b4b 100644
--- a/.gitignore
+++ b/.gitignore
@@ -1,51 +1,171 @@
-# Dependencias
-node_modules/
-.npm/
-
-# Entorno
+# ============================================================================
+# 🔐 SEGURIDAD - VARIABLES DE ENTORNO Y SECRETOS
+# ============================================================================
 .env
 .env.local
 .env.*.local
-!.env.example
+.env.production
+.env.staging
+.env.test
+.env.*.backup
+.secrets/
+secrets.json
+*.key
+*.pem
+*.pfx
+*.cert
+.ssh/
+
+# ============================================================================
+# 🔐 CREDENCIALES Y TOKENS
+# ============================================================================
+**/credentials.json
+**/credentials-*.json
+**/tokens.json
+**/auth-tokens/**
+**/.credentials/
+**/.auth/
+**/oauth-cache/
+**/google-auth/
+**/microsoft-auth/
 
-# Build y Logs
+# ============================================================================
+# 📦 DEPENDENCIAS Y MÓDULOS
+# ============================================================================
+node_modules/
+.npm/
+package-lock.json
+npm-debug.log*
+yarn.lock
+*.tgz
 dist/
 build/
+
+# ============================================================================
+# 📝 LOGS Y DIAGNÓSTICOS
+# ============================================================================
 *.log
-docker-compose.override.yml
-npm-debug.log*
+logs/
+*.log.*
+npm-debug.log
+yarn-debug.log
+yarn-error.log
+server/logs/
+client/logs/
+.pnpm-debug.log
+
+# ============================================================================
+# 💾 BASES DE DATOS
+# ============================================================================
+*.db
+*.sqlite
+*.sqlite3
+*.bson
+mongo/
+**/mongodb-backups/
+**/db-backups/
+db/
 
-# Sistema
+# ============================================================================
+# 🎨 IDE Y EDITOR
+# ============================================================================
+.vscode/
+.idea/
+*.swp
+*.swo
+*.swn
+*~
 .DS_Store
 Thumbs.db
+.project
+.classpath
+.c9/
+*.sublime-workspace
 
-# Temporales de Agente
+# ============================================================================
+# 🌐 CACHÉ Y TEMPORALES
+# ============================================================================
+.cache/
+**/.cache/
 tmp/
 temp/
-screenshots/
-bot/output/
 /server/.cache/
-server/logs/
+/server/.puppeteerrc.cjs
+/client/.next/
+/client/out/
+.next/
+build-cache/
+
+# ============================================================================
+# 🤖 AGENTES Y BOTS
+# ============================================================================
+bot/output/
+bot/screenshots/
+screenshots/
+.agents/
 .claude/
-node_modules
-node_modules/
-client/node_modules/
-server/node_modules/
-.env
-node_modules/
-**/node_modules/
-.cache/
-**/.cache/
-.env
-*.log
-/server/.cache/
-/client/build/
-/server/logs/
+.qodo/
 
-# Backups y Datos Pesados
-**/mongodb-backups/
-**/~/*/
-~/**
-*.bson
+# ============================================================================
+# 📊 DATOS SENSIBLES
+# ============================================================================
 *.json.gz
+*.json.bak
+*.backup
+data/
+exports/
+**/test-data/
+**/fixture-data/
+.test-db/
+
+# ============================================================================
+# 🐳 DOCKER Y DEPLOYMENT
+# ============================================================================
+docker-compose.override.yml
+docker-compose.local.yml
+.dockerignore
+.env.docker
+
+# ============================================================================
+# 🚀 BUILD OUTPUTS
+# ============================================================================
+dist/
+build/
+out/
+coverage/
+.next/
+.turbo/
+.vercel/
+
+# ============================================================================
+# 📁 DIRECTORIOS ESPECÍFICOS (PERMITIR SOLO EJEMPLOS)
+# ============================================================================
+# Permitir solo archivos .example
+!.env.example
+!.env.example.*
+!config.example.js
+
+# ============================================================================
+# 🔒 ARCHIVOS ESPECÍFICOS A IGNORAR
+# ============================================================================
+server/.env
+server/.env.*
+client/.env
+client/.env.*
+mobile/.env
+mobile/.env.*
+docker/.env
+
+# Archivos de prueba que contengan datos reales
+**/test-credentials.js
+**/real-api-keys.js
+
+# ============================================================================
+# 📱 MOBILE
+# ============================================================================
+mobile/node_modules/
+mobile/build/
+mobile/dist/
+mobile/android/app/build/
+mobile/ios/Pods/
 *.metadata.json
diff --git a/client/.gitignore b/client/.gitignore
index 4d29575de..8121d6cfb 100644
--- a/client/.gitignore
+++ b/client/.gitignore
@@ -1,23 +1,96 @@
-# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.
+# ============================================================================
+# 🔐 SEGURIDAD - VARIABLES DE ENTORNO
+# ============================================================================
+.env
+.env.local
+.env.development.local
+.env.test.local
+.env.production.local
+.env.*.backup
+.secrets/
+secrets.json
 
-# dependencies
+# ============================================================================
+# 📦 DEPENDENCIAS
+# ============================================================================
 /node_modules
 /.pnp
 .pnp.js
+node_modules/
+yarn.lock
+package-lock.json
 
-# testing
-/coverage
-
-# production
+# ============================================================================
+# 🔨 BUILD Y COMPILACIÓN
+# ============================================================================
 /build
+/dist
+/out
+.next/
+.vercel/
+coverage/
 
-# misc
-.DS_Store
-.env.local
-.env.development.local
-.env.test.local
-.env.production.local
-
+# ============================================================================
+# 📝 LOGS
+# ============================================================================
 npm-debug.log*
 yarn-debug.log*
 yarn-error.log*
+*.log
+
+# ============================================================================
+# 🎨 IDE
+# ============================================================================
+.vscode/
+.idea/
+*.swp
+*.swo
+*~
+.project
+.classpath
+
+# ============================================================================
+# 💻 OS
+# ============================================================================
+.DS_Store
+Thumbs.db
+
+# ============================================================================
+# 🌐 CACHÉ
+# ============================================================================
+.cache/
+.eslintcache
+.stylelintcache
+.turbo/
+
+# ============================================================================
+# 🧪 TESTING
+# ============================================================================
+/coverage
+.nyc_output/
+
+# ============================================================================
+# 🚀 DEPLOYMENT
+# ============================================================================
+.vercel
+.netlify
+
+# ============================================================================
+# 📱 EXPO/REACT-NATIVE
+# ============================================================================
+.expo/
+.expo-shared/
+dist/
+*.jks
+*.p8
+*.p12
+*.key
+*.mobileprovision
+*.orig.*
+
+# ============================================================================
+# 🔒 CREDENCIALES
+# ============================================================================
+**/credentials.json
+**/auth-tokens/
+google-service-account.json
diff --git a/client/src/platforms/comunicaciones/comunicacionesApi.js b/client/src/platforms/comunicaciones/comunicacionesApi.js
index f583b1723..86580ff4f 100644
--- a/client/src/platforms/comunicaciones/comunicacionesApi.js
+++ b/client/src/platforms/comunicaciones/comunicacionesApi.js
@@ -53,6 +53,7 @@ export const chatApi = {
     createRoom: (data) => comunicacionesApi.post('/rooms/create', data),
     searchUsers: (query) => comunicacionesApi.get(`/users/search?q=${query}`),
     getContacts: () => comunicacionesApi.get('/users/contacts'),
+    sendTyping: (roomId, data) => comunicacionesApi.post(`/typing/${roomId}`, data),
 
     // Estados
     getStatuses: () => comunicacionesApi.get('/status'),
diff --git a/client/src/platforms/comunicaciones/pages/Chat360.jsx b/client/src/platforms/comunicaciones/pages/Chat360.jsx
index b59b03fd7..6526019ce 100644
--- a/client/src/platforms/comunicaciones/pages/Chat360.jsx
+++ b/client/src/platforms/comunicaciones/pages/Chat360.jsx
@@ -23,6 +23,7 @@ const Chat360 = () => {
     const [inputText, setInputText] = useState('');
     const [searchTerm, setSearchTerm] = useState('');
     const [isLoading, setIsLoading] = useState(false);
+    const [typingUsers, setTypingUsers] = useState([]);
     const [sidebarTab, setSidebarTab] = useState('chats'); // 'chats' | 'contacts' | 'agenda'
     const [mainView, setMainView] = useState('chat'); // 'chat' | 'status' | 'announcements'
     
@@ -83,6 +84,9 @@ const Chat360 = () => {
 
     const messagesEndRef = useRef(null);
     const eventSourceRef = useRef(null);
+    const localTypingTimeoutRef = useRef(null);
+    const typingUsersTimeoutsRef = useRef({});
+    const typingStateRef = useRef(false);
 
     const visibleContacts = useMemo(() => {
         if (!user) return contacts;
@@ -187,6 +191,54 @@ const Chat360 = () => {
                         if (parsed.data.senderRef?._id !== user._id) {
                             new Audio('https://assets.mixkit.co/active_storage/sfx/2358/2358-preview.mp3').play().catch(() => {});
                         }
+                        setRooms(prev => prev.map(room => {
+                            const roomId = activeRoom._id || activeRoom.id;
+                            if (String(room._id || room.id) === String(roomId)) {
+                                return { ...room, unreadCount: 0, lastMessage: parsed.data };
+                            }
+                            return room;
+                        }));
+                    }
+
+                    if (parsed.type === 'typing') {
+                        const payload = parsed.data;
+                        if (String(payload.roomId) !== String(activeRoom._id || activeRoom.id) || payload.userId === user._id) return;
+
+                        setTypingUsers(prev => {
+                            const exists = prev.find(u => String(u.userId) === String(payload.userId));
+                            if (exists) return prev;
+                            return [...prev, { userId: payload.userId, name: payload.name }];
+                        });
+
+                        if (typingUsersTimeoutsRef.current[payload.userId]) {
+                            clearTimeout(typingUsersTimeoutsRef.current[payload.userId]);
+                        }
+                        typingUsersTimeoutsRef.current[payload.userId] = setTimeout(() => {
+                            setTypingUsers(prev => prev.filter(u => String(u.userId) !== String(payload.userId)));
+                            delete typingUsersTimeoutsRef.current[payload.userId];
+                        }, 1800);
+                    }
+
+                    if (parsed.type === 'message_read') {
+                        const payload = parsed.data;
+                        if (String(payload.roomId) === String(activeRoom._id || activeRoom.id)) {
+                            setRooms(prev => prev.map(room => {
+                                if (String(room._id || room.id) === String(payload.roomId)) {
+                                    return { ...room, unreadCount: 0 };
+                                }
+                                return room;
+                            }));
+
+                            setMessages(prev => prev.map(msg => {
+                                if (String(msg.senderRef?._id) === String(user._id)) {
+                                    const alreadyRead = Array.isArray(msg.isReadBy) && msg.isReadBy.some(id => String(id) === String(payload.userId));
+                                    if (!alreadyRead) {
+                                        return { ...msg, isReadBy: [...(msg.isReadBy || []), payload.userId] };
+                                    }
+                                }
+                                return msg;
+                            }));
+                        }
                     }
                 } catch (e) {
                     console.error("Error parsing chat message:", e);
@@ -210,6 +262,7 @@ const Chat360 = () => {
                 const res = await chatApi.getMessages(activeRoom._id || activeRoom.id);
                 setMessages(res.data);
                 scrollToBottom();
+                markRoomAsRead(activeRoom);
             } catch (e) { console.error("Error historial:", e); }
             finally { setIsLoading(false); }
         };
@@ -220,9 +273,55 @@ const Chat360 = () => {
         return () => {
             if (es) es.close();
             if (reconnectTimeout) clearTimeout(reconnectTimeout);
+            if (localTypingTimeoutRef.current) clearTimeout(localTypingTimeoutRef.current);
+            Object.values(typingUsersTimeoutsRef.current).forEach(clearTimeout);
+            typingUsersTimeoutsRef.current = {};
         };
     }, [activeRoom, user]);
 
+    const markRoomAsRead = async (room) => {
+        if (!room) return;
+        try {
+            await chatApi.markAsRead({ roomId: room._id || room.id });
+            setRooms(prev => prev.map(r => {
+                if (String(r._id || r.id) === String(room._id || room.id)) {
+                    return { ...r, unreadCount: 0 };
+                }
+                return r;
+            }));
+        } catch (e) {
+            console.error('Error marcando sala como leída:', e);
+        }
+    };
+
+    const sendTypingStatus = async (isTyping) => {
+        if (!activeRoom || !user) return;
+        try {
+            if (typingStateRef.current === isTyping) return;
+            typingStateRef.current = isTyping;
+            await chatApi.sendTyping(activeRoom._id || activeRoom.id, { isTyping });
+        } catch (e) {
+            console.error('Error enviando estado de escritura:', e);
+        }
+    };
+
+    useEffect(() => {
+        setTypingUsers([]);
+        typingStateRef.current = false;
+        Object.values(typingUsersTimeoutsRef.current).forEach(clearTimeout);
+        typingUsersTimeoutsRef.current = {};
+    }, [activeRoom]);
+
+    const handleInputChange = (e) => {
+        setInputText(e.target.value);
+        if (!activeRoom) return;
+        if (!typingStateRef.current) sendTypingStatus(true);
+        if (localTypingTimeoutRef.current) clearTimeout(localTypingTimeoutRef.current);
+        localTypingTimeoutRef.current = setTimeout(() => {
+            sendTypingStatus(false);
+        }, 1200);
+    };
+
     const scrollToBottom = () => {
         setTimeout(() => { messagesEndRef.current?.scrollIntoView({ behavior: "smooth" }); }, 100);
     };
@@ -237,6 +336,7 @@ const Chat360 = () => {
                 type: 'text'
             });
             setInputText('');
+            sendTypingStatus(false);
         } catch (e) { console.error("Error enviado mensaje:", e); }
     };
 
@@ -303,6 +403,7 @@ const Chat360 = () => {
         if (existing) {
             setActiveRoom(existing);
             setSidebarTab('chats');
+            markRoomAsRead(existing);
         } else {
             // Crear nueva sala directa
             try {
@@ -314,6 +415,7 @@ const Chat360 = () => {
                 setRooms(prev => [res.data, ...prev]);
                 setActiveRoom(res.data);
                 setSidebarTab('chats');
+                markRoomAsRead(res.data);
             } catch (e) {
                 console.error("Error creando chat directo:", e);
             }
@@ -438,7 +540,10 @@ const Chat360 = () => {
                         visibleRooms.filter(r => r.name.toLowerCase().includes(searchTerm.toLowerCase())).map(room => (
                             <div 
                                 key={room._id || room.id}
-                                onClick={() => setActiveRoom(room)}
+                                onClick={() => {
+                                    setActiveRoom(room);
+                                    markRoomAsRead(room);
+                                }}
                                 className={`p-4 flex items-center gap-3 cursor-pointer transition-all border-b border-gray-50 hover:bg-gray-50 ${activeRoom?._id === room._id ? 'bg-indigo-50 border-l-4 border-l-indigo-600' : ''}`}
                             >
                                 <div className="relative">
@@ -450,13 +555,20 @@ const Chat360 = () => {
                                     )}
                                 </div>
                                 <div className="flex-1 min-w-0">
-                                    <div className="flex justify-between items-baseline">
+                                    <div className="flex justify-between items-baseline gap-2">
                                         <h4 className="text-sm font-black text-gray-800 uppercase truncate tracking-tight">{room.name}</h4>
                                         <span className="text-[10px] text-gray-400 font-bold">{formatTime(room.updatedAt)}</span>
                                     </div>
-                                    <p className="text-xs text-gray-500 truncate mt-0.5 font-medium">
-                                        {room.lastMessage?.text || room.description || 'Inicia la conversación...'}
-                                    </p>
+                                    <div className="flex items-center justify-between gap-3">
+                                        <p className="text-xs text-gray-500 truncate mt-0.5 font-medium flex-1">
+                                            {room.lastMessage?.text || room.description || 'Inicia la conversación...'}
+                                        </p>
+                                        {room.unreadCount > 0 && (
+                                            <span className="inline-flex items-center justify-center min-w-[22px] h-6 rounded-full bg-indigo-600 text-white text-[10px] font-black uppercase tracking-widest px-2">
+                                                {room.unreadCount > 9 ? '9+' : room.unreadCount}
+                                            </span>
+                                        )}
+                                    </div>
                                 </div>
                             </div>
                         ))
@@ -518,6 +630,11 @@ const Chat360 = () => {
                                 <MoreVertical size={20} className="cursor-pointer hover:text-indigo-600 transition-colors" />
                             </div>
                         </div>
+                        {typingUsers.length > 0 && (
+                            <div className="px-4 py-2 bg-indigo-50 border border-indigo-100 rounded-2xl text-xs font-black uppercase tracking-widest text-indigo-700 mx-3 mt-3">
+                                {typingUsers.map((u) => u.name).join(', ')} {typingUsers.length === 1 ? 'está escribiendo...' : 'están escribiendo...'}
+                            </div>
+                        )}
 
                         <div className="flex-1 overflow-y-auto p-6 space-y-4" style={{background: 'radial-gradient(circle, #d9e8d9 1px, transparent 1px) center / 20px 20px, #e5ded8'}}>
                             {isLoading ? (
@@ -563,7 +680,11 @@ const Chat360 = () => {
                                                     <p className="text-sm font-medium leading-relaxed">{msg.text}</p>
                                                     <div className="flex items-center justify-end gap-1 mt-1">
                                                         <span className={`text-[8px] font-bold uppercase ${isMe ? 'text-indigo-200' : 'text-gray-400'}`}>{formatTime(msg.createdAt)}</span>
-                                                        {isMe && <CheckCheck size={10} className="text-indigo-200" />}
+                                                        {isMe && (
+                                                            <span className="flex items-center gap-1 text-[9px] uppercase font-black text-indigo-200">
+                                                                {msg.isReadBy?.length > 1 ? 'Leído' : 'Enviado'} <CheckCheck size={10} className="text-indigo-200" />
+                                                            </span>
+                                                        )}
                                                     </div>
                                                 </div>
                                             )}
@@ -583,7 +704,8 @@ const Chat360 = () => {
                                     placeholder="Escribe tu mensaje aquí..."
                                     className="flex-1 bg-white border-none py-3 px-5 rounded-2xl shadow-sm focus:ring-2 focus:ring-indigo-100 outline-none text-sm"
                                     value={inputText}
-                                    onChange={(e) => setInputText(e.target.value)}
+                                    onChange={handleInputChange}
+                                    onBlur={() => sendTypingStatus(false)}
                                 />
                                 <button type="submit" className="w-12 h-12 rounded-2xl flex items-center justify-center bg-indigo-600 text-white shadow-lg"><Send size={20} /></button>
                             </form>
diff --git a/google-deploy.sh b/google-deploy.sh
index bb7c7f3e8..88c0f2ff0 100755
--- a/google-deploy.sh
+++ b/google-deploy.sh
@@ -25,6 +25,31 @@ gcloud services enable run.googleapis.com cloudbuild.googleapis.com --quiet
 echo -e "${BLUE}📦 [1/2] Procesando BACKEND...${NC}"
 # Usamos copia manual para máxima compatibilidad con tu versión de gcloud
 cp docker/server.Dockerfile ./Dockerfile
+
+# Require secret env vars for deployment instead of hardcoding them.
+: "${MONGO_URI:?MONGO_URI debe exportarse antes de ejecutar este script}"
+: "${JWT_SECRET:?JWT_SECRET debe exportarse antes de ejecutar este script}"
+: "${SMTP_PASSWORD:?SMTP_PASSWORD debe exportarse antes de ejecutar este script}"
+: "${SEED_ADMIN_PASSWORD:?SEED_ADMIN_PASSWORD debe exportarse antes de ejecutar este script}"
+: "${TOA_PASS_REAL:?TOA_PASS_REAL debe exportarse antes de ejecutar este script}"
+: "${GPS_PASS:?GPS_PASS debe exportarse antes de ejecutar este script}"
+
+CLOUDINARY_CLOUD_NAME="${CLOUDINARY_CLOUD_NAME:-dcjct45jm}"
+CLOUDINARY_API_KEY="${CLOUDINARY_API_KEY:-657489544971689}"
+CLOUDINARY_API_SECRET="${CLOUDINARY_API_SECRET:-}"
+TOA_URL="${TOA_URL:-https://telefonica-cl.etadirect.com/}"
+TOA_USER_REAL="${TOA_USER_REAL:-16411496}"
+GPS_URL="${GPS_URL:-https://www.gpsimple.cl/track/index.php}"
+GPS_USER="${GPS_USER:-asalinas@skytelcom.cl}"
+SMTP_HOST="${SMTP_HOST:-smtppro.zoho.com}"
+SMTP_PORT="${SMTP_PORT:-465}"
+SMTP_EMAIL="${SMTP_EMAIL:-genai@synoptyk.cl}"
+FROM_NAME="${FROM_NAME:-Enterprise Platform}"
+ENABLE_AUTO_SEED="${ENABLE_AUTO_SEED:-false}"
+NODE_ENV="${NODE_ENV:-production}"
+
+# Shell parameter expansion above fails fast if required secrets are missing.
+
 gcloud run deploy genai-server \
     --source . \
     --region $REGION \
@@ -32,27 +57,27 @@ gcloud run deploy genai-server \
     --memory 2Gi \
     --cpu 1 \
     --port 8080 \
-    --set-env-vars "MONGO_URI=mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin" \
-    --set-env-vars "JWT_SECRET=YYuFufyipIiHXWAxNQoxYXPiL6jQhGV1fsIvwwaJaRc=" \
+    --set-env-vars "MONGO_URI=${MONGO_URI}" \
+    --set-env-vars "JWT_SECRET=${JWT_SECRET}" \
     --set-env-vars "JWT_EXPIRES_IN=30d" \
-    --set-env-vars "CLOUDINARY_CLOUD_NAME=dcjct45jm" \
-    --set-env-vars "CLOUDINARY_API_KEY=657489544971689" \
-    --set-env-vars "CLOUDINARY_API_SECRET=kOnxzw9a3tb5Y5J4PmUZLDYAOqg" \
-    --set-env-vars "TOA_URL=https://telefonica-cl.etadirect.com/" \
-    --set-env-vars "TOA_USER_REAL=16411496" \
-    --set-env-vars "TOA_PASS_REAL=Sinsajo10918." \
-    --set-env-vars "GPS_URL=https://www.gpsimple.cl/track/index.php" \
-    --set-env-vars "GPS_USER=asalinas@skytelcom.cl" \
-    --set-env-vars "GPS_PASS=qRX8kjQj" \
-    --set-env-vars "SMTP_HOST=smtppro.zoho.com" \
-    --set-env-vars "SMTP_PORT=465" \
-    --set-env-vars "SMTP_EMAIL=genai@synoptyk.cl" \
-    --set-env-vars "SMTP_PASSWORD=Genai_2026.." \
-    --set-env-vars "FROM_NAME=Enterprise Platform" \
-    --set-env-vars "SEED_ADMIN_EMAIL=admin@platform-os.cl" \
-    --set-env-vars "SEED_ADMIN_PASSWORD=BarrientosJobsMosk" \
-    --set-env-vars "ENABLE_AUTO_SEED=false" \
-    --set-env-vars "NODE_ENV=production" \
+    --set-env-vars "CLOUDINARY_CLOUD_NAME=${CLOUDINARY_CLOUD_NAME}" \
+    --set-env-vars "CLOUDINARY_API_KEY=${CLOUDINARY_API_KEY}" \
+    --set-env-vars "CLOUDINARY_API_SECRET=${CLOUDINARY_API_SECRET}" \
+    --set-env-vars "TOA_URL=${TOA_URL}" \
+    --set-env-vars "TOA_USER_REAL=${TOA_USER_REAL}" \
+    --set-env-vars "TOA_PASS_REAL=${TOA_PASS_REAL}" \
+    --set-env-vars "GPS_URL=${GPS_URL}" \
+    --set-env-vars "GPS_USER=${GPS_USER}" \
+    --set-env-vars "GPS_PASS=${GPS_PASS}" \
+    --set-env-vars "SMTP_HOST=${SMTP_HOST}" \
+    --set-env-vars "SMTP_PORT=${SMTP_PORT}" \
+    --set-env-vars "SMTP_EMAIL=${SMTP_EMAIL}" \
+    --set-env-vars "SMTP_PASSWORD=${SMTP_PASSWORD}" \
+    --set-env-vars "FROM_NAME=${FROM_NAME}" \
+    --set-env-vars "SEED_ADMIN_EMAIL=${SEED_ADMIN_EMAIL}" \
+    --set-env-vars "SEED_ADMIN_PASSWORD=${SEED_ADMIN_PASSWORD}" \
+    --set-env-vars "ENABLE_AUTO_SEED=${ENABLE_AUTO_SEED}" \
+    --set-env-vars "NODE_ENV=${NODE_ENV}" \
     --clear-base-image \
     --quiet
 rm ./Dockerfile
diff --git a/mobile/app.json b/mobile/app.json
index 954a71842..a685c33aa 100644
--- a/mobile/app.json
+++ b/mobile/app.json
@@ -4,8 +4,10 @@
     "slug": "genai-mobile",
     "version": "1.0.1",
     "orientation": "portrait",
+    "icon": "./assets/icon.png",
     "userInterfaceStyle": "light",
     "splash": {
+      "image": "./assets/splash.png",
       "resizeMode": "contain",
       "backgroundColor": "#0f172a"
     },
@@ -21,6 +23,7 @@
     ],
     "ios": {
       "supportsTablet": true,
+      "bundleIdentifier": "com.synoptik.genai360",
       "infoPlist": {
         "NSLocationWhenInUseUsageDescription": "Esta aplicación necesita tu ubicación para registrar actividades, inspecciones y firma de documentos.",
         "NSLocationAlwaysAndWhenInUseUsageDescription": "Esta aplicación necesita acceso a tu ubicación para registrar tus actividades operativas, firmar documentos y funcionar correctamente en terreno."
@@ -30,6 +33,7 @@
       "package": "com.synoptik.genai360",
       "versionCode": 3,
       "adaptiveIcon": {
+        "foregroundImage": "./assets/adaptive-icon.png",
         "backgroundColor": "#0f172a"
       },
       "permissions": [
diff --git a/mobile/package.json b/mobile/package.json
index ca240c40f..9c4674b14 100644
--- a/mobile/package.json
+++ b/mobile/package.json
@@ -10,16 +10,16 @@
     "web": "expo start --web"
   },
   "dependencies": {
+    "@react-navigation/native": "^6.1.18",
+    "@react-navigation/native-stack": "^6.11.0",
+    "axios": "^1.7.7",
     "expo": "~51.0.28",
-    "expo-status-bar": "~1.12.1",
     "expo-location": "~17.0.1",
+    "expo-status-bar": "~1.12.1",
     "react": "18.2.0",
     "react-native": "0.74.5",
-    "@react-navigation/native": "^6.1.18",
-    "@react-navigation/native-stack": "^6.11.0",
     "react-native-safe-area-context": "4.10.5",
-    "react-native-screens": "3.31.1",
-    "axios": "^1.7.7"
+    "react-native-screens": "3.31.1"
   },
   "devDependencies": {
     "@babel/core": "^7.25.2"
diff --git a/package-lock.json b/package-lock.json
index 35baaf1a8..a48e49385 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -283,9 +283,9 @@
       "license": "BSD-2-Clause"
     },
     "node_modules/lodash": {
-      "version": "4.17.23",
-      "resolved": "https://registry.npmjs.org/lodash/-/lodash-4.17.23.tgz",
-      "integrity": "sha512-LgVTMpQtIopCi79SJeDiP0TfWi5CNEc/L/aRdTh3yIvmZXTnheWpKjSZhnvMl8iXbC1tFg9gdHHDMLoV7CnG+w==",
+      "version": "4.18.1",
+      "resolved": "https://registry.npmjs.org/lodash/-/lodash-4.18.1.tgz",
+      "integrity": "sha512-dMInicTPVE8d1e5otfwmmjlxkZoUpiVLwyeTdUsi/Caj/gfzzblBcCE5sRHV/AsjuCmxWrte2TNGSYuCeCq+0Q==",
       "dev": true,
       "license": "MIT"
     },
@@ -482,9 +482,9 @@
       "peer": true
     },
     "node_modules/shell-quote": {
-      "version": "1.8.3",
-      "resolved": "https://registry.npmjs.org/shell-quote/-/shell-quote-1.8.3.tgz",
-      "integrity": "sha512-ObmnIF4hXNg1BqhnHmgbDETF8dLPCggZWBjkQfhZpbszZnYur5DUljTcCHii5LC3J5E0yeO/1LIMyH+UvHQgyw==",
+      "version": "1.8.4",
+      "resolved": "https://registry.npmjs.org/shell-quote/-/shell-quote-1.8.4.tgz",
+      "integrity": "sha512-VsC6n6vz1ihYYyZZwX7YZSF5l5x36ca17OC+a69h94YqB7X6XLwf+5MOgynYir2SLFUbl8gIYvBo8K8RoNQ6bQ==",
       "dev": true,
       "license": "MIT",
       "engines": {
diff --git a/package.json b/package.json
index 48f7e5b5e..c9c9daa12 100644
--- a/package.json
+++ b/package.json
@@ -23,7 +23,14 @@
     "deploy:msg": "git add . && git commit && git push",
     "deploy:google": "./google-deploy.sh",
     "deploy:full": "npm run deploy && npm run deploy:google",
-    "restart": "lsof -ti:5003,3000 | xargs kill -9 2>/dev/null || true && npm run dev"
+    "restart": "lsof -ti:5003,3000 | xargs kill -9 2>/dev/null || true && npm run dev",
+    "security:audit": "node scripts/security-audit.js",
+    "security:check-env": "node -e \"require('./server/utils/envValidator').initializeEnvironment()\"",
+    "security:install-hook": "cp scripts/pre-commit-security-hook.sh .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit",
+    "audit:npm": "npm audit --json && echo '✅ Audit complete'",
+    "audit:all": "npm run security:audit && npm run audit:npm",
+    "prebuild": "npm run security:audit",
+    "predeploy": "npm run security:audit && npm run security:check-env"
   },
   "devDependencies": {
     "concurrently": "^8.2.2"
diff --git a/scratch/check_juan_duran.js b/scratch/check_juan_duran.js
index e980c72ba..117f01062 100644
--- a/scratch/check_juan_duran.js
+++ b/scratch/check_juan_duran.js
@@ -1,6 +1,6 @@
 const { MongoClient } = require('mongodb');
 
-const mongoUri = 'mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin&directConnection=true';
+const mongoUri = process.env.MONGO_URI || process.env.MONGODB_URI || 'mongodb://localhost:27017/genai';
 
 async function checkWorker() {
     const client = new MongoClient(mongoUri);
diff --git a/scratch/check_ot.js b/scratch/check_ot.js
index 5cfabd2d6..61cffae9e 100644
--- a/scratch/check_ot.js
+++ b/scratch/check_ot.js
@@ -4,7 +4,7 @@ const path = require('path');
 
 dotenv.config({ path: path.join(__dirname, '../server/.env') });
 
-const MONGO_URI = process.env.MONGO_URI || "mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin";
+const MONGO_URI = process.env.MONGO_URI || process.env.MONGODB_URI || 'mongodb://localhost:27017/genai';
 
 async function run() {
     console.log("Connecting to MongoDB...");
diff --git a/scratch/list_users_all.js b/scratch/list_users_all.js
index a558cc4a4..b669c6f15 100644
--- a/scratch/list_users_all.js
+++ b/scratch/list_users_all.js
@@ -1,6 +1,6 @@
 const { MongoClient } = require('mongodb');
 
-const MONGO_URI = 'mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin';
+const MONGO_URI = process.env.MONGO_URI || process.env.MONGODB_URI || 'mongodb://localhost:27017/genai';
 
 async function listUsers() {
   const client = new MongoClient(MONGO_URI);
diff --git a/scratch/test_resolve.js b/scratch/test_resolve.js
index c198aca24..b58d05e50 100644
--- a/scratch/test_resolve.js
+++ b/scratch/test_resolve.js
@@ -4,7 +4,7 @@ const path = require('path');
 
 dotenv.config({ path: path.join(__dirname, '../server/.env') });
 
-const MONGO_URI = process.env.MONGO_URI || "mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin&directConnection=true";
+const MONGO_URI = process.env.MONGO_URI || process.env.MONGODB_URI || 'mongodb://localhost:27017/genai';
 
 async function run() {
     console.log("Connecting to MongoDB...");
diff --git a/server/.gitignore b/server/.gitignore
index e6873f58c..58d740159 100644
--- a/server/.gitignore
+++ b/server/.gitignore
@@ -1,36 +1,124 @@
-# Logs
+# ============================================================================
+# 🔐 SEGURIDAD - VARIABLES DE ENTORNO Y CREDENCIALES
+# ============================================================================
+.env
+.env.local
+.env.production
+.env.staging
+.env.test
+.env.*.backup
+.secrets/
+secrets.json
+*.key
+*.pem
+*.pfx
+*.cert
+.ssh/
+
+# ============================================================================
+# 🔐 CREDENCIALES DE TERCEROS
+# ============================================================================
+**/credentials.json
+**/credentials-*.json
+**/tokens.json
+**/oauth-cache/
+google-auth-*.json
+microsoft-auth-*.json
+auth-cache/
+.auth/
+
+# ============================================================================
+# 📝 LOGS
+# ============================================================================
 logs/
 *.log
 npm-debug.log*
+yarn-debug.log*
+yarn-error.log*
+.pnpm-debug.log
+combined-*.log
+error-*.log
+server.log
 
-# Environment variables
-.env
-.env.local
-.env.*.local
+# ============================================================================
+# 📦 DEPENDENCIAS
+# ============================================================================
+node_modules/
+.npm/
+package-lock.json
+yarn.lock
+.yarn/
 
-# Puppeteer cache
+# ============================================================================
+# 🎨 IDE Y EDITOR
+# ============================================================================
+.vscode/
+.idea/
+*.swp
+*.swo
+*.swn
+*~
+.project
+.classpath
+.c9/
+*.sublime-workspace
+
+# ============================================================================
+# 🌐 CACHÉ
+# ============================================================================
 .cache/
 .puppeteer_cache/
+.puppeteerrc.cjs
+/cache/
+.next/
 
-# Temporary files
+# ============================================================================
+# 📊 TEMPORALES Y TEST
+# ============================================================================
 tmp/
 temp/
 *.tmp
+test-*.js
+test-*.log
+*.bak
+*.backup
 
-# IDE
-.vscode/
-.idea/
-*.swp
-*.swo
-*~
+# ============================================================================
+# 💾 BASES DE DATOS
+# ============================================================================
+*.db
+*.sqlite
+*.sqlite3
+*.bson
+mongo/
+
+# ============================================================================
+# 🐳 DOCKER
+# ============================================================================
+docker-compose.override.yml
+.env.docker
+
+# ============================================================================
+# 🔨 BUILD Y COMPILACIÓN
+# ============================================================================
+dist/
+build/
+coverage/
 
-# OS
+# ============================================================================
+# 💻 OS
+# ============================================================================
 .DS_Store
 Thumbs.db
 
-# Test coverage
-coverage/
+# ============================================================================
+# 🤖 BOTS Y AGENTES
+# ============================================================================
+bot/output/
+screenshots/
 
-# Build artifacts
-dist/
-build/
+# ============================================================================
+# 📁 DIRECTORIOS ESPECÍFICOS
+# ============================================================================
+platforms/*/node_modules/
+middleware/*/node_modules/
diff --git a/server/analizar-duplicados.js b/server/analizar-duplicados.js
index c5d4e686c..61fba10bf 100644
--- a/server/analizar-duplicados.js
+++ b/server/analizar-duplicados.js
@@ -1,7 +1,7 @@
 const mongoose = require('mongoose');
 require('dotenv').config({ path: '/Users/mauro/Synoptik_Innovacion/Gen AI/server/.env' });
 
-const MONGO_URI = process.env.MONGO_URI || 'mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin';
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function analizar() {
   await mongoose.connect(MONGO_URI);
diff --git a/server/check.js b/server/check.js
index 96cec7343..a2afc2377 100644
--- a/server/check.js
+++ b/server/check.js
@@ -1,6 +1,6 @@
 const mongoose = require('mongoose');
 const fs = require('fs');
-const MONGO_URI = 'mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin';
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function run() {
     try {
diff --git a/server/check_endpoint.js b/server/check_endpoint.js
index 688f1b665..ff0d39d8f 100644
--- a/server/check_endpoint.js
+++ b/server/check_endpoint.js
@@ -1,6 +1,6 @@
 const { MongoClient } = require('mongodb');
 
-const MONGO_URI = 'mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin';
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function checkEndpoint() {
   const client = new MongoClient(MONGO_URI);
diff --git a/server/check_new_downloads.js b/server/check_new_downloads.js
index 3c5a2766f..eb2fecf3b 100644
--- a/server/check_new_downloads.js
+++ b/server/check_new_downloads.js
@@ -4,7 +4,7 @@ const path = require('path');
 
 dotenv.config({ path: path.join(__dirname, '.env') });
 
-const MONGO_URI = process.env.MONGO_URI || "mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin&directConnection=true";
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function run() {
     await mongoose.connect(MONGO_URI);
diff --git a/server/check_production.js b/server/check_production.js
index 0c2516abb..4171a0a65 100644
--- a/server/check_production.js
+++ b/server/check_production.js
@@ -1,6 +1,6 @@
 const { MongoClient } = require('mongodb');
 
-const MONGO_URI = 'mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin';
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function checkProduction() {
   const client = new MongoClient(MONGO_URI);
diff --git a/server/check_pts.js b/server/check_pts.js
index fe22ad3f2..e79144323 100644
--- a/server/check_pts.js
+++ b/server/check_pts.js
@@ -1,6 +1,6 @@
 const { MongoClient, ObjectId } = require('mongodb');
 
-const MONGO_URI = 'mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin';
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function checkPts() {
   const client = new MongoClient(MONGO_URI);
diff --git a/server/check_sin_estado.js b/server/check_sin_estado.js
index 11c58b96c..7bdee7b4c 100644
--- a/server/check_sin_estado.js
+++ b/server/check_sin_estado.js
@@ -1,6 +1,6 @@
 const { MongoClient, ObjectId } = require('mongodb');
 
-const MONGO_URI = 'mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin';
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function checkSinEstado() {
   const client = new MongoClient(MONGO_URI);
diff --git a/server/check_states.js b/server/check_states.js
index 31555cb70..54a47d6c4 100644
--- a/server/check_states.js
+++ b/server/check_states.js
@@ -1,6 +1,6 @@
 const { MongoClient } = require('mongodb');
 
-const MONGO_URI = 'mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin';
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function checkStates() {
   const client = new MongoClient(MONGO_URI);
diff --git a/server/debug_activities.js b/server/debug_activities.js
index 7fdf11090..5ae024cb0 100644
--- a/server/debug_activities.js
+++ b/server/debug_activities.js
@@ -1,6 +1,6 @@
 const { MongoClient } = require('mongodb');
 
-const MONGO_URI = 'mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin';
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function debugActivities() {
   const client = new MongoClient(MONGO_URI);
diff --git a/server/debug_mongo.js b/server/debug_mongo.js
index 56cf203b0..3bf5d6c53 100644
--- a/server/debug_mongo.js
+++ b/server/debug_mongo.js
@@ -1,5 +1,5 @@
 const mongoose = require('mongoose');
-mongoose.connect('mongodb+srv://ceo_synoptyk_reclutando:ReclutaSeguro.%23%23@clusterreclutando.im7etzo.mongodb.net/reclutando?retryWrites=true&w=majority&appName=ClusterReclutando');
+mongoose.connect(process.env.MONGO_URI || 'mongodb://localhost:27017/genai');
 
 async function run() {
   const Empresa = mongoose.model('Empresa', new mongoose.Schema({ nombre: String }));
diff --git a/server/find_collections.js b/server/find_collections.js
index 978b139ea..7cd534986 100644
--- a/server/find_collections.js
+++ b/server/find_collections.js
@@ -1,6 +1,6 @@
 const { MongoClient } = require('mongodb');
 
-const MONGO_URI = 'mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin';
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function findCollections() {
   const client = new MongoClient(MONGO_URI);
diff --git a/server/find_keys.js b/server/find_keys.js
index 0ee09ba57..bf5cab8b0 100644
--- a/server/find_keys.js
+++ b/server/find_keys.js
@@ -4,7 +4,7 @@ const path = require('path');
 
 dotenv.config({ path: path.join(__dirname, '.env') });
 
-const MONGO_URI = process.env.MONGO_URI || "mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin&directConnection=true";
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function run() {
     await mongoose.connect(MONGO_URI);
diff --git a/server/find_user_genai.js b/server/find_user_genai.js
index 41ab20f82..a73f0a41b 100644
--- a/server/find_user_genai.js
+++ b/server/find_user_genai.js
@@ -1,6 +1,6 @@
 const { MongoClient, ObjectId } = require('mongodb');
 
-const MONGO_URI = 'mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin';
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function findUserGenai() {
   const client = new MongoClient(MONGO_URI);
diff --git a/server/fix_tecnicos.js b/server/fix_tecnicos.js
index ca36c697e..ad6fe5110 100644
--- a/server/fix_tecnicos.js
+++ b/server/fix_tecnicos.js
@@ -1,7 +1,7 @@
 const mongoose = require('mongoose');
 require('dotenv').config();
 
-const MONGO_URI = 'mongodb+srv://ceo_synoptyk_reclutando:ReclutaSeguro.%23%23@clusterreclutando.im7etzo.mongodb.net/reclutando?retryWrites=true&w=majority&appName=ClusterReclutando';
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 const cleanRut = (r) => (r || "").toString().replace(/[^0-9kK]/g, '').toUpperCase().trim();
 
diff --git a/server/inspect_fields.js b/server/inspect_fields.js
index 20ea6082d..b33f9ce03 100644
--- a/server/inspect_fields.js
+++ b/server/inspect_fields.js
@@ -4,7 +4,7 @@ const path = require('path');
 
 dotenv.config({ path: path.join(__dirname, '.env') });
 
-const MONGO_URI = process.env.MONGO_URI || "mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin&directConnection=true";
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function run() {
     console.log("Connecting to MongoDB...");
diff --git a/server/limpiar-duplicados.js b/server/limpiar-duplicados.js
index fddc2f487..e3791d88c 100644
--- a/server/limpiar-duplicados.js
+++ b/server/limpiar-duplicados.js
@@ -1,7 +1,7 @@
 const mongoose = require('mongoose');
 require('dotenv').config({ path: __dirname + '/.env' });
 
-const MONGO_URI = process.env.MONGO_URI || 'mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin';
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 // =============================================================================
 // MAPA DE LIMPIEZA: columna_a_eliminar → columna_canonica_a_mantener
diff --git a/server/limpiar_combustible.js b/server/limpiar_combustible.js
index 7ee7c91f2..823421ee1 100644
--- a/server/limpiar_combustible.js
+++ b/server/limpiar_combustible.js
@@ -1,7 +1,7 @@
 const mongoose = require('mongoose');
 require('dotenv').config({ path: __dirname + '/.env' });
 
-const MONGO_URI = process.env.MONGO_URI || 'mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin';
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 const ConsumoCombustible = require('./platforms/agentetelecom/models/ConsumoCombustible');
 
 async function cleanDuplicates() {
diff --git a/server/limpiar_manual.js b/server/limpiar_manual.js
index 2dfffa3f6..f8d229680 100644
--- a/server/limpiar_manual.js
+++ b/server/limpiar_manual.js
@@ -1,6 +1,6 @@
 const mongoose = require('mongoose');
 
-const uri = "mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin&directConnection=true";
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function limpiarBase() {
     try {
diff --git a/server/list_users.js b/server/list_users.js
index 9a4cd38ea..b861f4f7f 100644
--- a/server/list_users.js
+++ b/server/list_users.js
@@ -1,6 +1,6 @@
 const { MongoClient } = require('mongodb');
 
-const MONGO_URI = 'mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin';
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function listUsers() {
   const client = new MongoClient(MONGO_URI);
diff --git a/server/migrate_cargos.js b/server/migrate_cargos.js
index fdda7a71a..33a5fe41a 100644
--- a/server/migrate_cargos.js
+++ b/server/migrate_cargos.js
@@ -2,7 +2,7 @@ const mongoose = require('mongoose');
 const dotenv = require('dotenv');
 dotenv.config();
 
-const uri = process.env.MONGO_URI || "mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin&directConnection=true";
+const uri = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 mongoose.connect(uri)
   .then(async () => {
diff --git a/server/platforms/auth/seedCeo.js b/server/platforms/auth/seedCeo.js
index 36f95786f..3850c4446 100644
--- a/server/platforms/auth/seedCeo.js
+++ b/server/platforms/auth/seedCeo.js
@@ -8,8 +8,8 @@ const PlatformUser = require('./PlatformUser');
 
 const CEO_DATA = {
     name: 'Mauricio Barrientos',
-    email: 'admin@platform-os.cl',
-    password: 'Platform2026*ADMIN',
+    email: process.env.CEO_EMAIL || 'admin@platform-os.cl',
+    password: process.env.CEO_PASSWORD,
     role: 'system_admin',
     cargo: 'CEO & Fundador',
     status: 'Activo',
@@ -21,6 +21,12 @@ const CEO_DATA = {
 };
 
 async function seed() {
+    if (!CEO_DATA.password) {
+        console.error('❌ ERROR: CEO_PASSWORD no definida en el entorno. No se puede crear el usuario CEO.');
+        process.exit(1);
+        return;
+    }
+
     try {
         await mongoose.connect(process.env.MONGO_URI);
         console.log('✅ Conectado a MongoDB');
@@ -33,7 +39,7 @@ async function seed() {
             await PlatformUser.create({ ...CEO_DATA, tokenVersion: 1 });
             console.log('🚀 Usuario CEO creado exitosamente:');
             console.log('   Email:', CEO_DATA.email);
-            console.log('   Password:', CEO_DATA.password);
+            console.log('   Password: [hidden]');
             console.log('   ⚠️  CAMBIA LA CONTRASEÑA EN PRODUCCIÓN');
         }
     } catch (e) {
diff --git a/server/platforms/comunicaciones/controllers/chatController.js b/server/platforms/comunicaciones/controllers/chatController.js
index fef45be40..40b957348 100644
--- a/server/platforms/comunicaciones/controllers/chatController.js
+++ b/server/platforms/comunicaciones/controllers/chatController.js
@@ -183,6 +183,12 @@ exports.sendMessage = async (req, res) => {
 
         await newMsg.save();
 
+        if (roomObj) {
+            roomObj.lastMessage = newMsg._id;
+            roomObj.updatedAt = new Date();
+            await roomObj.save();
+        }
+
         // Broadcast vía SSE a todos los interesados (mismo roomId o contexto empresa)
         const populatedMsg = await Message.findById(newMsg._id).populate('senderRef', 'name cargo role isOnline avatar');
         
@@ -231,6 +237,40 @@ exports.markAsRead = async (req, res) => {
             { $push: { isReadBy: userId } }
         );
 
+        clients.forEach(client => {
+            if (client.roomId === roomId) {
+                client.res.write(`data: ${JSON.stringify({ type: 'message_read', data: { roomId, userId } })}\n\n`);
+            }
+        });
+
+        res.json({ success: true });
+    } catch (error) {
+        res.status(500).json({ error: error.message });
+    }
+};
+
+exports.sendTyping = async (req, res) => {
+    try {
+        const { roomId } = req.params;
+        const { isTyping } = req.body;
+        const user = req.user;
+
+        const payload = {
+            type: 'typing',
+            data: {
+                roomId,
+                isTyping: !!isTyping,
+                userId: user._id,
+                name: user.name || 'Alguien'
+            }
+        };
+
+        clients.forEach(client => {
+            if (client.roomId === roomId && String(client.userId) !== String(user._id)) {
+                client.res.write(`data: ${JSON.stringify(payload)}\n\n`);
+            }
+        });
+
         res.json({ success: true });
     } catch (error) {
         res.status(500).json({ error: error.message });
@@ -366,6 +406,21 @@ exports.getRooms = async (req, res) => {
             return others.every(id => canUserContact(user, memberMap.get(String(id))));
         });
 
+        const roomIds = rooms.map(room => String(room._id || room.id));
+        const unreadCounts = roomIds.length > 0 ? await Message.aggregate([
+            { $match: { roomId: { $in: roomIds }, isReadBy: { $ne: user._id } } },
+            { $group: { _id: '$roomId', count: { $sum: 1 } } }
+        ]) : [];
+        const unreadMap = new Map(unreadCounts.map(item => [String(item._id), item.count]));
+
+        rooms = rooms.map(room => {
+            const roomObj = room.toObject ? room.toObject() : room;
+            return {
+                ...roomObj,
+                unreadCount: unreadMap.get(String(roomObj._id || roomObj.id)) || 0
+            };
+        });
+
         res.json(rooms);
     } catch (error) {
         res.status(500).json({ error: error.message });
diff --git a/server/platforms/comunicaciones/routes/chatRoutes.js b/server/platforms/comunicaciones/routes/chatRoutes.js
index 0a6c9b05c..01ef8fea2 100644
--- a/server/platforms/comunicaciones/routes/chatRoutes.js
+++ b/server/platforms/comunicaciones/routes/chatRoutes.js
@@ -11,6 +11,7 @@ router.post('/send', protect, chatController.sendMessage);
 
 // Marcar como leídos
 router.post('/read', protect, chatController.markAsRead);
+router.post('/typing/:roomId', protect, chatController.sendTyping);
 
 // Stream Real-time (SSE)
 router.get('/stream/global', protect, chatController.globalStream);
diff --git a/server/platforms/comunicaciones/webmailRoutes.js b/server/platforms/comunicaciones/webmailRoutes.js
index d227a542e..6675a0310 100644
--- a/server/platforms/comunicaciones/webmailRoutes.js
+++ b/server/platforms/comunicaciones/webmailRoutes.js
@@ -188,7 +188,7 @@ async function getImapClient(account) {
             pass = account.imapPassword;
             // Migrar automáticamente a encriptación
             account.passwordEncrypted = encryptText(account.imapPassword);
-            account.save().catch(e => console.error("Error migrating legacy IMAP password:", e));
+            account.save().catch(e => console.error("Error migrating legacy IMAP credentials:", e));
         }
 
         client = new ImapFlow({
@@ -717,7 +717,7 @@ router.post('/send/:id', protect, authorize('social_webmail'), async (req, res)
                 pass = account.smtpPassword;
                 // Migrar automáticamente a encriptación
                 account.passwordEncrypted = encryptText(account.smtpPassword);
-                account.save().catch(e => console.error("Error migrating legacy SMTP password:", e));
+                account.save().catch(e => console.error("Error migrating legacy SMTP credentials:", e));
             }
 
             transporterConfig = {
diff --git a/server/scratch/check_cargo_cat.js b/server/scratch/check_cargo_cat.js
index 1aba22c19..71714b526 100644
--- a/server/scratch/check_cargo_cat.js
+++ b/server/scratch/check_cargo_cat.js
@@ -1,8 +1,10 @@
 const mongoose = require('mongoose');
-const URI = 'mongodb+srv://ceo_synoptyk_reclutando:ReclutaSeguro.%23%23@clusterreclutando.im7etzo.mongodb.net/reclutando?retryWrites=true&w=majority&appName=ClusterReclutando';
+require('dotenv').config({ path: require('path').join(__dirname, '../.env') });
+
+const URI = process.env.MONGO_URI || 'mongodb://localhost:27017/reclutando';
 
 // Connect to MongoDB
-mongoose.connect(URI);
+mongoose.connect(URI, { useNewUrlParser: true, useUnifiedTopology: true });
 
 // Define schemas manually to avoid loading files and registering them out of order
 const CategoriaSchema = new mongoose.Schema({
diff --git a/server/scratch/check_collections_mayo.js b/server/scratch/check_collections_mayo.js
index 0969973c4..3353b4c3b 100644
--- a/server/scratch/check_collections_mayo.js
+++ b/server/scratch/check_collections_mayo.js
@@ -1,6 +1,6 @@
 
 const { MongoClient } = require('mongodb');
-const MONGO_URI = 'mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin&directConnection=true';
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function check() {
   const client = new MongoClient(MONGO_URI);
diff --git a/server/scratch/clear_production.js b/server/scratch/clear_production.js
index 54212f1e1..6edd90fff 100644
--- a/server/scratch/clear_production.js
+++ b/server/scratch/clear_production.js
@@ -1,6 +1,7 @@
 const mongoose = require('mongoose');
+require('dotenv').config({ path: require('path').join(__dirname, '../.env') });
 
-const mongoURI = "mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin&directConnection=true";
+const mongoURI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function deepClean() {
   try {
diff --git a/server/scripts/relink-8-9.js b/server/scripts/relink-8-9.js
index 6480bacc6..15f531e7a 100644
--- a/server/scripts/relink-8-9.js
+++ b/server/scripts/relink-8-9.js
@@ -1,7 +1,7 @@
 const mongoose = require('mongoose');
 require('dotenv').config({ path: __dirname + '/../.env' });
 
-const MONGO_URI = process.env.MONGO_URI || 'mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin';
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 const Tecnico = require('../platforms/agentetelecom/models/Tecnico');
 const Actividad = require('../platforms/agentetelecom/models/Actividad');
diff --git a/server/server.js b/server/server.js
index 210f1215e..27c7d8a17 100644
--- a/server/server.js
+++ b/server/server.js
@@ -577,11 +577,19 @@ if (!process.env.MONGO_URI) {
         }
 
         const existing = await PlatformUser.findOne({ email: ceoEmail });
+        const seedPassword = process.env.SEED_ADMIN_PASSWORD || (process.env.NODE_ENV !== 'production' ? 'Platform2026*Master' : undefined);
+
         if (!existing) {
+          if (!seedPassword) {
+            console.error('❌ ERROR: SEED_ADMIN_PASSWORD no definida. No se puede crear el CEO en producción.');
+            process.exit(1);
+            return;
+          }
+
           const ceo = new PlatformUser({
             name: 'Mauricio Barrientos',
             email: ceoEmail,
-            password: process.env.SEED_ADMIN_PASSWORD || 'Platform2026*Master',
+            password: seedPassword,
             role: 'system_admin',
             cargo: 'System Administrator',
             status: 'Activo',
diff --git a/server/test_empresa.js b/server/test_empresa.js
index 5d33600ae..1a8c919ee 100644
--- a/server/test_empresa.js
+++ b/server/test_empresa.js
@@ -4,7 +4,7 @@ const Schema = new mongoose.Schema({}, { strict: false });
 const Candidato = mongoose.model('Candidato', Schema, 'candidatos');
 
 async function run() {
-  await mongoose.connect('mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin');
+  await mongoose.connect(process.env.MONGO_URI || 'mongodb://localhost:27017/genai');
   
   const cands = await Candidato.find({ fullName: /VICENTE/i }).lean();
   console.log('Vicente empresaRef:', cands.map(c => c.empresaRef));
diff --git a/server/test_ids.js b/server/test_ids.js
index 4861b5751..ec8725d4b 100644
--- a/server/test_ids.js
+++ b/server/test_ids.js
@@ -1,5 +1,5 @@
 const mongoose = require('mongoose');
-mongoose.connect('mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin').then(async () => {
+mongoose.connect(process.env.MONGO_URI || 'mongodb://localhost:27017/genai').then(async () => {
   const C = mongoose.model('Candidato', new mongoose.Schema({}, { strict: false }));
   const docs = await C.find({}).lean();
   console.log(`Total candidatos: ${docs.length}`);
diff --git a/server/test_ids2.js b/server/test_ids2.js
index d597b411c..966497737 100644
--- a/server/test_ids2.js
+++ b/server/test_ids2.js
@@ -4,7 +4,7 @@ const Schema = new mongoose.Schema({}, { strict: false });
 const Actividad = mongoose.model('Actividad', Schema, 'actividades');
 
 async function run() {
-  await mongoose.connect('mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin');
+  await mongoose.connect(process.env.MONGO_URI || 'mongodb://localhost:27017/genai');
   
   const query = {
       fecha: { $gte: new Date('2026-06-01T00:00:00Z'), $lt: new Date('2026-07-01T00:00:00Z') },
diff --git a/server/test_joaquin.js b/server/test_joaquin.js
index eefca5627..d8c8d66ec 100644
--- a/server/test_joaquin.js
+++ b/server/test_joaquin.js
@@ -1,7 +1,7 @@
 const mongoose = require('mongoose');
 const Candidato = require('./platforms/rrhh/models/Candidato');
 
-mongoose.connect('mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin&directConnection=true')
+mongoose.connect(process.env.MONGO_URI || 'mongodb://localhost:27017/genai')
   .then(async () => {
     const cand = await Candidato.find({ 
       $or: [
diff --git a/server/test_joaquin_tecnico.js b/server/test_joaquin_tecnico.js
index 931f5381c..c48c9255c 100644
--- a/server/test_joaquin_tecnico.js
+++ b/server/test_joaquin_tecnico.js
@@ -1,7 +1,7 @@
 const mongoose = require('mongoose');
 const Tecnico = require('./platforms/agentetelecom/models/Tecnico');
 
-mongoose.connect('mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin&directConnection=true')
+mongoose.connect(process.env.MONGO_URI || 'mongodb://localhost:27017/genai')
   .then(async () => {
     const tec = await Tecnico.find({ idRecursoToa: "28710" }).lean();
     console.log(JSON.stringify(tec, null, 2));
diff --git a/server/test_match.js b/server/test_match.js
index 1893fec1f..aa23cd418 100644
--- a/server/test_match.js
+++ b/server/test_match.js
@@ -6,7 +6,7 @@ const Candidato = mongoose.model('Candidato', Schema, 'candidatos');
 const Actividad = mongoose.model('Actividad', Schema, 'actividades');
 
 async function run() {
-  await mongoose.connect('mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin');
+  await mongoose.connect(process.env.MONGO_URI || 'mongodb://localhost:27017/genai');
   
   // Simulated techMap
   const techMap = {};
diff --git a/server/test_matias.js b/server/test_matias.js
index 2be367103..dd66e319e 100644
--- a/server/test_matias.js
+++ b/server/test_matias.js
@@ -1,7 +1,7 @@
 const mongoose = require('mongoose');
 const Candidato = require('./platforms/rrhh/models/Candidato');
 
-mongoose.connect('mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin&directConnection=true')
+mongoose.connect(process.env.MONGO_URI || 'mongodb://localhost:27017/genai')
   .then(async () => {
     const cand = await Candidato.find({ fullName: /MAT.AS/i }).lean();
     console.log(cand.map(c => c.fullName));
diff --git a/server/test_resolve.js b/server/test_resolve.js
index 35a08c9af..083ddb9bd 100644
--- a/server/test_resolve.js
+++ b/server/test_resolve.js
@@ -4,7 +4,7 @@ const path = require('path');
 
 dotenv.config({ path: path.join(__dirname, '.env') });
 
-const MONGO_URI = process.env.MONGO_URI || "mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin&directConnection=true";
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function run() {
     console.log("Connecting to MongoDB in server context...");
diff --git a/server/test_resolve_db.js b/server/test_resolve_db.js
index 5d1b78a34..8f5f3732c 100644
--- a/server/test_resolve_db.js
+++ b/server/test_resolve_db.js
@@ -4,7 +4,7 @@ const path = require('path');
 
 dotenv.config({ path: path.join(__dirname, '.env') });
 
-const MONGO_URI = process.env.MONGO_URI || "mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin&directConnection=true";
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function run() {
     console.log("Connecting to MongoDB in server context...");
diff --git a/server/test_schema.js b/server/test_schema.js
index eb3f665a2..de8d8f62b 100644
--- a/server/test_schema.js
+++ b/server/test_schema.js
@@ -1,5 +1,5 @@
 const mongoose = require('mongoose');
-mongoose.connect('mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin').then(async () => {
+mongoose.connect(process.env.MONGO_URI || 'mongodb://localhost:27017/genai').then(async () => {
   const model = require('./models/Candidato'); // assuming it's in models/
   console.log('Schema for empresaRef:', model.schema.paths.empresaRef);
   process.exit(0);
diff --git a/server/test_vicente.js b/server/test_vicente.js
index 56b10ece7..4429628d2 100644
--- a/server/test_vicente.js
+++ b/server/test_vicente.js
@@ -4,7 +4,7 @@ const Schema = new mongoose.Schema({}, { strict: false });
 const Actividad = mongoose.model('Actividad', Schema, 'actividades');
 
 async function run() {
-  await mongoose.connect('mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin');
+  await mongoose.connect(process.env.MONGO_URI || 'mongodb://localhost:27017/genai');
   
   const query = {
       fecha: { $gte: new Date('2026-06-01T00:00:00Z'), $lt: new Date('2026-07-01T00:00:00Z') },
diff --git a/server/verify_dates.js b/server/verify_dates.js
index ca6fd9459..5fe8aacaa 100644
--- a/server/verify_dates.js
+++ b/server/verify_dates.js
@@ -1,6 +1,6 @@
 const { MongoClient } = require('mongodb');
 
-const MONGO_URI = 'mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin';
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function verifyDates() {
   const client = new MongoClient(MONGO_URI);
diff --git a/server/wipe89.js b/server/wipe89.js
index 4148b0751..28f404063 100644
--- a/server/wipe89.js
+++ b/server/wipe89.js
@@ -1,7 +1,7 @@
 require('dotenv').config();
 const mongoose = require('mongoose');
 
-const MONGO_URI = 'mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin';
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function run() {
     await mongoose.connect(MONGO_URI);
diff --git a/server/wipeCombustible.js b/server/wipeCombustible.js
index ec0143aaf..950ee094b 100644
--- a/server/wipeCombustible.js
+++ b/server/wipeCombustible.js
@@ -1,7 +1,7 @@
 const mongoose = require('mongoose');
 const ConsumoCombustible = require('./platforms/agentetelecom/models/ConsumoCombustible');
 
-const MONGO_URI = 'mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin';
+const MONGO_URI = process.env.MONGO_URI || 'mongodb://localhost:27017/genai';
 
 async function wipe() {
     try {
diff --git a/test_lpu.js b/test_lpu.js
index 489c998ab..4c1afbb4e 100644
--- a/test_lpu.js
+++ b/test_lpu.js
@@ -1,6 +1,8 @@
 const mongoose = require('mongoose');
 const TarifaLPU = require('./server/platforms/agentetelecom/models/TarifaLPU');
-mongoose.connect('mongodb://adminReclutando:SecureMongo2026.%23@34.27.229.165:27017/genai?authSource=admin&directConnection=true')
+const MONGO_URI = process.env.MONGO_URI || process.env.MONGODB_URI || 'mongodb://localhost:27017/genai';
+
+mongoose.connect(MONGO_URI)
   .then(async () => {
     const lpus = await TarifaLPU.find().lean();
     console.log(JSON.stringify(lpus.map(l => ({desc: l.descripcion, code: l.codigo})), null, 2));
