{
  "name": "Genai",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run server:prod",
    "server": "npm start --prefix server",
    "server:prod": "PUPPETEER_CACHE_DIR=./server/.cache/puppeteer node server/server.js",
    "client": "npm start --prefix client",
    "build": "npm install --prefix server && PUPPETEER_CACHE_DIR=./server/.cache/puppeteer npx puppeteer browsers install chrome@127.0.6533.88",
    "bot": "npm run bot --prefix server",
    "gen": "concurrently \"npm run server\" \"npm run client\"",
    "deploy": "git add . && git commit -m 'Update: production sync' && git push",
    "deploy:msg": "git add . && git commit && git push"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "lucide-react": "^0.577.0",
    "react-leaflet": "^5.0.0"
  }
}
